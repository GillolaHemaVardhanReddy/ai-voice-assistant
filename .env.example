# Copy this file to `.env` and fill in real values. `.env` is gitignored.

# The "brain" — get one at https://console.anthropic.com  (Phase 1)
ANTHROPIC_API_KEY=sk-ant-...

# We'll add STT / TTS keys in later phases as needed.
