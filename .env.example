# Copy this file to `.env` and fill in real values. `.env` is gitignored.

# The "brain" — we use OpenRouter (one key, many models incl. Claude). Get one at https://openrouter.ai  (Phase 1)
OPENROUTER_API_KEY=sk-or-...

# Optional: direct Anthropic access — https://console.anthropic.com
# ANTHROPIC_API_KEY=sk-ant-...

# We'll add STT / TTS keys in later phases as needed.
